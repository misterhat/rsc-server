// This file is empty on purpose. There's no reason to check for pings as a
// timeout measure as `net` already does this automatically.

module.exports.name = 'ping';

module.exports.handle = function () { };
